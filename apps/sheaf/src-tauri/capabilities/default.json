{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window. Phase 0 needs only the core defaults; every plugin permission added later must be justified in its PR.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
