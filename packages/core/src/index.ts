/**
 * @sheaf/core: platform-independent domain logic.
 *
 * Empty in Phase 0 on purpose. Phase 1 adds the project format (document
 * files, frontmatter, binder ordering, migrations) here, behind a small
 * file-system interface that the shell implements. See README.md.
 */
export {};
